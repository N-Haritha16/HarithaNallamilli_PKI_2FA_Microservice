services:
  app:
    image: harithanallamilli_pki_2fa_microservice-app
    build:
      context: .
      dockerfile: Dockerfile
    container_name: totp_app
    ports:
      - "8000:8000"
    env_file:
      - .env.dev
    command: python app.py
